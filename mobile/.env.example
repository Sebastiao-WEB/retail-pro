# Opcional: só usado uma vez se ainda não houver servidor configurado no dispositivo.
# Em produção o endereço define-se na interface do app (subdomínio ou URL).
# Ex.: EXPO_PUBLIC_API_URL=https://padaria.mozretailpos.com/api/v1
# EXPO_PUBLIC_API_URL=
