# Use localhost só no simulador iOS ou web.
# No telemóvel/emulador Android o app detecta o IP do Metro (ex.: 192.168.x.x).
# Backend: php artisan serve --host=0.0.0.0 --port=8000
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1
